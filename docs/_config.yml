title: go-mx
description: Type-safe, component-based HTML generation for Go — with a shadcn/ui port.
theme: jekyll-theme-cayman
show_downloads: false

# The component gallery under /gallery is pre-rendered static HTML (committed,
# generated by `go run ./cmd/shadcn-gallery -out docs/gallery -base /go-mx/gallery`).
# It carries no front matter, so Jekyll copies it through verbatim and the theme
# layout is not applied to it.
